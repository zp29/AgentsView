{
  "name": "agentsview-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AgentsView monorepo — web console + macOS AgentsBar.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "start:source": "npm --prefix web run start:source",
    "task": "npm --prefix web run task --",
    "token": "npm --prefix web run token --",
    "hooks:print": "npm --prefix web run hooks:print --",
    "hooks:install": "npm --prefix web run hooks:install --",
    "test": "npm --prefix web run test --",
    "check": "npm --prefix web run check"
  }
}
